{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const input = process.env.CLAUDE_TOOL_INPUT || ''; const path = (input.match(/file_path[\\\"']\\s*[:=]\\s*[\\\"']([^\\\"']+)/i) || [])[1] || ''; if (/\\.csproj$/i.test(path) && !/Tests?\\.csproj$/i.test(path)) { const fs = require('fs'); const testProj = 'Tests/Tests.csproj'; if (fs.existsSync(testProj)) { const content = fs.readFileSync(testProj, 'utf8'); const projName = path.replace(/.*[\\/\\\\]/, '').replace(/\\.csproj$/, ''); if (!content.includes(projName)) { console.error('WARNING: New provider project ' + projName + ' is not referenced in Tests/Tests.csproj. Remember to add a ProjectReference.'); } } }\""
          }
        ]
      }
    ]
  }
}
